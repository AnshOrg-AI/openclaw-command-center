{
  "$schema": "./dashboard.schema.json",
  "_comment": "Example: JWT Authentication Configuration",

  "server": {
    "port": 3333,
    "host": "localhost"
  },

  "auth": {
    "mode": "jwt",
    "token": "your-jwt-secret-here",
    "allowedUsers": ["david"],
    "allowedIPs": [],
    "publicPaths": ["/login", "/api/login", "/api/health", "/api/whoami", "/favicon.ico"]
  }
}
